## project management
- let's divide the work into two parts:
	- input parsing + output structuring
	- the stack structure (double linked list + helper funcs for printing it) + the algorithms

## code related
- let's use an enum for the operations that are more descriptive than the two/three cmd letters

